# Dependencias de runtime para Streamlit Community Cloud.
# Rangos flexibles a propósito: el cloud elige Python/plataforma y los pins
# exactos pueden no tener wheel ahí. La reproducibilidad del entorno de
# desarrollo la garantiza pyproject.toml (versiones exactas).
pandas>=2.2,<3
pandera>=0.24
pyarrow>=17
streamlit>=1.40
python-dotenv>=1.0
requests>=2.32
